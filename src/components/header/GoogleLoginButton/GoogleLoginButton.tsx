import "./GoogleLoginButton.scss";
import { Button } from "../../shared/Button/Button";

function GoogleLoginButton() {
	return (
		<Button onClick={() => {}} className="google__button button" title="Login">
			Prisijungti su Google
		</Button>
	);
}

export { GoogleLoginButton };
